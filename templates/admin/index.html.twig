{% extends 'base.html.twig' %}

{% block body %}
	<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 p-4 gap-4">
		<a href='{{ path('app_admin_users_index') }}' class="card flex-col justify-between gap-6 h-full bg-cover rtl:bg-[left_top_-1.7rem] bg-[right_top_-1.7rem] bg-no-repeat channel-stats-bg">
			<div class="flex flex-col gap-1 pb-4 px-5">
				<span class="ki-solid ki-users text-4xl mt-2"></span>
				<span class="text-3xl font-semibold text-gray-900">
					{{ user_count }}
				</span>
				<span class="text-2sm font-normal text-gray-700">
					{% trans %}Users{% endtrans %}
				</span>
			</div>
		</a>
	</div>
{% endblock %}
